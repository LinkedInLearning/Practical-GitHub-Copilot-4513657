# Copilotによって自動生成されたコードです。必ずしも同じコードが生成されるとは限りません。
def ReadFileaAndPrintName():
    # Read the file
    file = open("sample.txt", "r")

    # Print the name of the file
    print(file.name)

    # Close the file
    file.close()